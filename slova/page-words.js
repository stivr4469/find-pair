/**
 * SlovaPages.words — WordsPage (виртуальный список)
 * Stub for Stage 1 (fills in during Stage 2 - Agent B)
 */

window.SlovaPages = window.SlovaPages || {};
window.SlovaPages.words = {
  render: function (root, ctx) {
    root.innerHTML = '<div class="slova-stub view-enter"><p>Слова — в работе</p></div>';
  },
  destroy: function () {}
};
