/**
 * SlovaPages.learn — LearnPage + TrainingCard + ProgressBar + Timer
 * Stub for Stage 1 (fills in during Stage 2 - Agent A)
 */

window.SlovaPages = window.SlovaPages || {};
window.SlovaPages.learn = {
  render: function (root, ctx) {
    root.innerHTML = '<div class="slova-stub view-enter"><p>Учить — в работе</p></div>';
  },
  destroy: function () {}
};
