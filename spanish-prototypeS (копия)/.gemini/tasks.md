# Список задач 

 - [x] Этап 1: Настройка структуры проекта и инициализация системы отслеживания
 - [x] Этап 2: Создание компонентов пользовательского интерфейса
 - [x] Этап 3: Реализация логики работы с формулами
  - [x] Расширение упражнений для Формулы 1 (Существительное + два прилагательных)
  - [x] Модификация `App.jsx` и `Formula1.jsx` для последовательного отображения упражнений
  - [x] Исправление ошибки "Rendered more hooks than during the previous render" и "Missing initializer in destructuring declaration" в `Formula1.jsx`
  - [x] Удаление дублирующегося кода в `Formula1.jsx`
  - [x] Полное удаление "(Пример X)" из русских и "(Example X)" из испанских предложений в `exercises.js`
  - [x] Корректировка логики фильтрации упражнений в `App.jsx`
  - [x] Очистка кэша и переустановка зависимостей
  - [x] Добавление отладочных логов
  - [x] Исправление вызова `onNextExercise` в `Formula1.jsx`
  - [x] Проблема решена: Приложение работает корректно.
  - [x] Удаление отладочных логов
  - [x] Полное удаление "(Пример X)" из русских и "(Example X)" из испанских предложений в `exercises.js`
 - [ ] Этап 4: Интеграция с Tailwind CSS
 - [ ] Этап 5: Создание структуры данных
 - [ ] Этап 6: Разработка игровых механик
  - [x] Реализовано перемещение слов по клику
 - [ ] Этап 7: Тестирование и отладка
 - [ ] Этап 8: Подготовка к интеграции 

### Детальный план интеграции (Main Project)

**Цель:** Интегрировать модуль изучения формул в основной проект (`main project`).

**Этап 1: Перенос файлов**

*   [x] 1.1. **Копирование компонентов:** Скопировать `FormulaPracticeScreen`, `FormulaMenu`, `FormulaCard`, `Formula1` из `integration/components` в `main project/src/components`.
*   [x] 1.2. **Копирование данных:** Скопировать папку `data` из `integration` в `main project/src`.

**Этап 2: Интеграция UI — Добавление кнопки в меню**

*   [x] 2.1. **Чтение `LessonMenu.jsx`:** Прочитать содержимое файла `main project/src/components/LessonMenu.jsx`.
*   [x] 2.2. **Добавление кнопки:** В `LessonMenu.jsx` добавить кнопку "20 формул" (ранее "Изучение формул").
*   [x] 2.3. **Проверка:** Визуально проверить код `LessonMenu.jsx`.

**Этап 3: Интеграция логики — Запуск модуля по клику**

*   [x] 3.1. **Чтение `App.jsx`:** Прочитать файл `main project/src/App.jsx`.
*   [x] 3.2. **Создание обработчика:** В `App.jsx` создать функцию `handleShowFormulas`.
*   [x] 3.3. **Передача обработчика:** Передать `handleShowFormulas` в `LessonMenu`.
*   [x] 3.4. **Связывание кнопки:** В `LessonMenu.jsx` использовать `handleShowFormulas` для `onClick`.

**Этап 4: Интеграция логики — Отображение модуля**

*   [x] 4.1. **Добавление импорта:** В `main project/src/App.jsx` импортировать `FormulaPracticeScreen`.
*   [x] 4.2. **Условный рендеринг:** В `App.jsx` добавить логику для отображения `FormulaPracticeScreen`.
*   [x] 4.3. **Реализация возврата:** Добавить в `FormulaPracticeScreen` пропс `onBackToMenu`.

**Этап 5: Зависимости и стили**

*   [x] 5.1. **Анализ зависимостей:** Проверить `main project/package.json` на наличие `@dnd-kit/core` и других необходимых зависимостей.
*   [x] 5.2. **Анализ стилей:** Проверить `main project/tailwind.config.cjs` и убедиться, что пути к новым компонентам включены.

**Этап 6: Улучшения и геймификация (Предложения)**

*   [x] 6.1. Загрузка данных формул и упражнений из Firebase.
*   [ ] 6.2. Подключить модуль к системе прогресса: вызывать функцию `addXP` из основного приложения при правильном выполнении упражнения.
*   [ ] 6.3. Реализовать механику "карточек для повторения": сохранять упражнения, в которых пользователь ошибся, для последующей практики.
*   [x] 6.3. Заменить системные `alert()` на встроенные в UI уведомления для сообщений о правильности ответа.
*   [x] 6.4. Добавить кнопку "Назад в меню" в `FormulaMenu.jsx` и дубликат в `Formula1.jsx`.